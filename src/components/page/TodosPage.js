import React from 'react';
import TodosListContainer from '../../todos-list/TodosList.container';

const todo1 = { id: 1, text: 'todo 1' };
export default () => (
    <dev>
        <h2>Todos List</h2>
        <TodosListContainer />
    </dev>
)